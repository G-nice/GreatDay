<resources>

    <!-- 同时隐藏状态栏和标题栏  AppTheme_HIDE  -->
    <!--<style name="AppTheme_HIDE" parent="android:Theme.Holo.Light.NoActionBar.Fullscreen">-->
    <!--<style name="AppTheme_HIDE" parent="android:Theme.Material.Light.NoActionBar.Fullscreen">-->
    <style name="AppTheme_HIDE" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Base application theme. -->
        <!--name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->

        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
